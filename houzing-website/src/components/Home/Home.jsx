import React from "react";
import { Wrapper, Container } from "./Home.styled";

export const Home = () => {
  return (
    <Wrapper>
      <Container>home</Container>
    </Wrapper>
  );
};

export default Home;
